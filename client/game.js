window.addEventListener('DOMContentLoaded', () => {

  const nameForm = document.querySelector('#enter-name')
  nameForm.addEventListener('submit', (e) => {
    // set name in localStorage
    // add player to game - they get an id
    // connect to socket
    // show game
  })
})